# 개발일지 — 2026-06-12 (스터디메이트 2주차 마감)

> 한 세션에서 오답노트 보관함부터 글로벌 교과 삼각편대·AI 정밀 플래너·진행도 배너·레거시 청소까지
> **8개 커밋 / 마이그레이션 4건(006–009)**을 처리하고 프로덕션 릴리즈 상태로 마감.

## 오늘의 결과 요약

| # | 커밋 | 한 줄 요약 |
|---|------|-----------|
| 1 | `e9d9bb2` | 학생용 영속적 오답노트 보관함 + 오답 지우개 재도전 루프 |
| 2 | `d29350e` | CERT 벌크 시드(43문항) + IELTS Reading P2 + SCHOOL `curriculum_code` 확장 베이스 |
| 3 | `fc9deba` | 006 passage 누락 버그 수정 + 원격 DB 마이그레이션 러너(`scripts/migrate.js`) |
| 4 | `de2629a` | AI 인터랙티브 정밀 플래너 — Study Budget 기반 마일스톤 로드맵 |
| 5 | `279c623` | 교과(SCHOOL) 글로벌 — KR 고등수학 + UK A-Level + 한국형 액센트 난이도 |
| 6 | `430a57f` | 교과 글로벌 삼각편대 완성 — 캐나다 온타리오(CA_ON_MATH) 트랙 |
| 7 | `aa7b7f5` | 대시보드 AI 페이스메이커 배너 — 플래너 진행도 실시간 연동 |
| 8 | `d3829d5` | 레거시 플래너 클러스터 전면 청소 — 프로덕션 릴리즈 정비 |

---

## 1. 영속적 오답노트 보관함 + 오답 지우개 (`e9d9bb2`)

- `(student)/student/incorrect/page.tsx`(서버): `user_attempts`를 본인 세션 소유 필터 + 최신순 조회 후 JS에서 문항별 그룹화 → **"가장 최근 시도가 `is_correct=false`인 문항"**만 추출(누적 오답 횟수 집계, ESSAY·정복 문항 제외).
- `IncorrectNotebook.tsx`(클라): 자격증 우선 3분류 탭 + 좌40%/우60% 스플릿(지문 고정·정오답 대조·인디고 해설 카드).
- **오답 지우개**: '다시 풀기' 미니 퀴즈 → 기존 `/api/grade` 재사용(복습용 단일 세션 생성) → 정답 시 초록 토스트 + 카드 클리어 애니메이션, DB 최신 시도가 정답이 되어 다음 로드부터 자동 제외.
- GNB '나의 오답노트' 링크 + 대시보드 바로가기로 펀널 연결.
- 경로 정정: 학생 페이지는 `(auth)`가 아니라 `(student)` 그룹(레이아웃 GNB 상속).

## 2. CERT 벌크 시드 + LANG 보강 + SCHOOL 확장 베이스 (`d29350e`, 마이그레이션 006)

- 신규 **43문항** 전량 한국어 해설 포함: 컴활(스프레드시트6+DB5), 정보처리기사(설계5+개발4+DB활용4), 한국사(선사~현대 5시대×3=15).
- LANG: IELTS Academic Reading **Passage 2**(약 650단어 오리지널 지문, 객관3+단답1) — `passage` 컬럼 전 Row 100% 복사 규칙 준수.
- SCHOOL 확장 베이스: `learning_chapters.curriculum_code`(nullable) + 부분 인덱스. `KR_SAT_`/`CA_ON_`/`UK_ALEVEL_` 접두어 규약을 SQL 주석 + `docs/school-curriculum-architecture.md`에 명문화(하위 호환, 앱 무영향).
- 멱등: 신규 `level_2` 라벨 집합으로만 스코프해 DELETE→재삽입.

## 3. 마이그레이션 러너 + 006 버그 수정 (`fc9deba`)

- `scripts/migrate.js`: `.env.local`의 `DATABASE_URL`을 읽어 `pg`로 `.sql` 직접 실행(트랜잭션 래핑·RAISE NOTICE 출력·자리표시자 대괄호 자동 제거·특수문자 분해 파싱·`PG_*_OVERRIDE` 우회).
- **인프라 트러블슈팅**: 다이렉트 호스트 `db.<ref>.supabase.co`가 **IPv6 전용**이라 IPv4 PC에서 `ENOTFOUND`. 9개 지역 자동 탐지 끝에 **싱가포르 `aws-1-ap-southeast-1.pooler.supabase.com`(Session Pooler)** 확정 → `.env.local`을 올바른 풀러 `DATABASE_URL`로 영구 수정.
- 006 IELTS INSERT에서 `passage` 값 4행 누락("컬럼>값") 발견 → 트랜잭션 롤백 덕에 무손상, 4행 보강 후 재실행 성공. 원격 DB에 43문항 적재 검증.

## 4. AI 인터랙티브 정밀 플래너 (`de2629a`, 마이그레이션 007)

- `ai_study_plans` 테이블(`exam_date`/`availability_matrix` JSONB/`plan_content`/`completed_items`, user+category 유니크, RLS).
- `lib/planner-budget.ts`: 오늘~시험일 요일별 가용시간 합산 = **Study Budget**(서버·클라 공용 순수 함수).
- `lib/ai/study-plan.ts`: 예산+`weakness_stats` → 마일스톤 로드맵 LLM 생성, 키부재/실패 시 예산·취약점 반영 **동적 mock 폴백**.
- `PlannerStudio.tsx`: 커스텀 날짜 선택 모달(과거일 차단) + 7열 요일별 가용시간 그리드 + 마일스톤 체크박스(즉시 저장→진행률 실시간) + [스케줄 재설정].
- 검증 중 **AI_API_KEY 401(무효 키)** 확인 → 현재 mock 폴백 가동, 유효 키 주입 시 무코드 전환.

## 5–6. 교과(SCHOOL) 글로벌 삼각편대 (`279c623`, `430a57f`, 마이그레이션 008·009)

- 단일 SCHOOL 카테고리 '교과 과외 (수학)' 안에 `curriculum_code`로 3트랙 구분:
  - **KR_HIGH_MATH**(4단원, 이차방정식 6문항) — 한국형 **액센트 난이도** `1등급 도전`/`2~3등급 굳히기`/`개념 다지기`.
  - **UK_ALEVEL_MATH**(3단원, Business Math 복리·감가상각·행렬 5문항, 영문+한국어 해설).
  - **CA_ON_MATH**(4단원, 지수함수·변화율 7문항, 영문+한국어 해설).
- `difficulty` CHECK 제약 **해제**(시장별 라벨 자유 텍스트, 상/중/하 호환). `lib/difficulty.ts` 공유 헬퍼로 4개 컴포넌트 일괄 적용(1등급 도전=인디고 강조, 미지 라벨 폴백).
- `lib/exams.ts` `buildExams`가 `curriculumCode` 전달 + `CURRICULUM_META`(🇰🇷/🇬🇧/🇨🇦 뱃지). 대시보드가 카탈로그를 트랙별 그룹 그리드로 시각 분리 렌더.
- **마이그레이션 격리 설계**: 008은 KR/UK 코드로만, 009는 CA 코드로만 스코프 정리 → 공유 카테고리에서 어느 쪽을 재실행해도 타 트랙 무손상.

## 7. 대시보드 AI 페이스메이커 배너 (`aa7b7f5`)

- 대시보드 서버 컴포넌트가 프로필 이름 + 최신 `ai_study_plans`(updated_at desc) 병렬 조회 → 마일스톤 총량 대비 `completed_items` 체크 비율로 달성률 계산.
- `PacemakerBanner.tsx`: 인디고 그라디언트, D-Day+달성률 결합 다이내믹 격려 문구, 0→pct 애니메이션 게이지바, [상세보기→] CTA. 플랜 없음 가드(유도 CTA).

## 8. 레거시 전면 청소 (`d3829d5`)

- 삭제(무참조 전수 추적): `WeaknessPlanner.tsx`, `CategoryTabs.tsx`, `lib/ai/planner.ts`, `/api/plan-generation`, 빈 라우트 디렉터리 4개, 레거시 타입(`PlannerInput`/`DailyPlan`/`AIStudyPlan`).
- `.next` 타입 캐시가 삭제 라우트를 물던 빌드 실패 → 캐시 클린 후 **경고·에러 0 클린 빌드**. 활성 라우트 19→18(죽은 plan-generation 제거).

---

## 인프라·품질 하이라이트

- **원격 DB 자동 마이그레이션 파이프라인** 확보(`scripts/migrate.js`) — 이후 006·007·008·009 모두 자동 적재.
- IPv6 다이렉트 불가 → **싱가포르 Session Pooler**로 영구 전환(`db_migration_runner` 메모리 기록).
- 모든 시드 마이그레이션 **멱등 + 트랙 격리** 원칙 일관 적용.
- 신규 문항 전량 **한국어 상세 해설 필수** 규칙 준수(오답노트 즉시 학습 가능).

## 빌드/배포 상태

- 최종 클린 빌드: **활성 라우트 18개, TypeScript 무결, 경고/에러 0**.
- main 최신: `d3829d5` (8개 커밋 모두 push 완료).

## 남은 과제 (다음 스프린트)

- `AI_API_KEY` 유효 키 발급 → 플래너·주관식 채점 mock→실LLM 전환 검증.
- AI 실시간 문제 생성(현재 사전 시드 조회), DELF·어학 서술형 실채점 확충.
- 진단 page `select('*')` answer 클라 노출 보안 정비.
- 교과 추가 확장(UK GCSE, KR 수능 타 과목, 캐나다 BC주 등) — `curriculum_code` 패턴 재사용.
